<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ECF FILM</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="view/web/css/style.css" rel="stylesheet">
  <link href="view/web/css/style.css" rel="stylesheet">
</head>


<body>
<main> <div id="myCarousel" class="carousel slide column" data-bs-ride="carousel">
  <div id="myCarousel" class="carousel slide column" data-bs-ride="carousel">
    <div class="carousel-inner">
      {% for film in films %}
<div class="carousel-item{% if loop.first %} active{% endif %}">
  <div class="row">
    <div class="col-md-6 d-flex flex-column justify-content-left">
      <h2 class="text">{{ film.id }}-{{ film.titre }}</h2>
      <p class="text">Réalisé en <strong>{{ film.annee }}</strong> par <strong>{{ film.realisateur }}</strong></p>
      {% for role in film.roles %}
      <p class="text"><em>{{ role }}</em></p>
      {% endfor %}
    </div>
    <div class="col-md-6 d-flex justify-content-center">
      <img class="d-block mx-auto w-50" src="{{ film.affiche }}" alt="{{ film.titre }}">
    </div>
  </div>
</div>
{% endfor %}

    </div>


  <div class="carousel-controls d-flex justify-content-between align-items-end my-3">
    <a class="carousel-control-prev" href="#myCarousel" role="button" data-bs-slide="prev">
      <button class="btn btn-primary">Preview</button>
    </a>
    <a class="carousel-control-next" href="#myCarousel" role="button" data-bs-slide="next">
      <button class="btn btn-primary">Next</button>
    </a>
  </div>
</div></main>




  <!-- Inclure le fichier JS de Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>